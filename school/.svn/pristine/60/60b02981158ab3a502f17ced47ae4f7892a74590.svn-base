Boilerplate for controller

```javascript
Ext.define('School.controller.YouController', {
	extend: 'Ext.app.Controller',
	requires: [
	],

	views: [
	],

	refs: [
	],

	init: function(application) {
		this.control({
			// 'selector': { 'event': this.methodName }
		});
	},

	// methodName: function(params) {}
});
```